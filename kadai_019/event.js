// 新しくbutton要素を作成し、定数に代入する
const btn = document.getElementById('btn');

// HTML要素がクリックされたときにイベント処理を実行する
btn.addEventListener('click', () => {
 
 //指定した要素に「ボタンがクリックされました」というテキストを追加
 text.textContent = 'ボタンがクリックされました';
});